# MS-TEAMS

MS TEAMS Team Activity: Badge Generator App

Objective:

Create Python app that can:
• Provides a CLI or GUI to display badges
 • Serves rechalenge Via Flask so other teams can consume them (use with
ngrok to expose publicly)
• Includes Git/GitHub usage notes and a full README.md for (documentation)

This project is a Badge Generator App built using Python.
It allows users to generate simple badge images containing the  Name.


User will enter name and an award to generate a badge automatically 

#REQUIREMENTS
-flask
-pillow (for image processing)
-badge (project name)
-badges (a folder where badge template images stored)
